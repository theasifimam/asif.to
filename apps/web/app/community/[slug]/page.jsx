import Header from "@/components/layout/Header"; import Footer from "@/components/layout/Footer"; import CommunityPostDetail from "@/components/community/CommunityPostDetail";
async function fetchPost(slug){try{const response=await fetch(`${process.env.NEXT_PUBLIC_API_URL}/community/posts/${encodeURIComponent(slug)}`,{next:{revalidate:30}});if(!response.ok)return null;return (await response.json())?.data?.post||null;}catch{return null}}
export async function generateMetadata({params}){const {slug}=await params;const post=await fetchPost(slug);if(!post)return{title:"Discussion unavailable",robots:{index:false,follow:false}};return{title:post.title,description:post.body.slice(0,160),alternates:{canonical:`/community/${post.slug}`},robots:{index:false,follow:true}}}
export default async function CommunityPostPage({params}){const {slug}=await params;return <div className="min-h-screen bg-zinc-50 text-zinc-950 dark:bg-zinc-950 dark:text-zinc-100"><Header/><main className="mx-auto max-w-4xl px-4 pb-24 pt-24 sm:px-6"><CommunityPostDetail slug={slug}/></main><Footer/></div>}
